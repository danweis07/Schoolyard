import type { ModuleManifest } from '@/lib/modules'

const manifest: ModuleManifest = {
  name: 'forms',
  navLabelKey: 'nav.forms',
  icon: 'heroicons:document-text',
  route: '/forms',
  implemented: true,
  tier: 'active-pta',
}

export default manifest
