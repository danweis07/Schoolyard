import type { ModuleManifest } from '@/lib/modules'

const manifest: ModuleManifest = {
  name: 'directory',
  navLabelKey: 'nav.directory',
  icon: 'heroicons:user-group',
  route: '/directory',
  implemented: true,
  tier: 'active-pta',
}

export default manifest
