# Classroom Module (stub)

To implement:

1. Build pages under `apps/web/src/pages/classroom/`:
   - `index.astro` — teacher directory
   - `[teacher].astro` — individual teacher pages
2. Add a `teachers` content collection in `apps/web/src/content/config.ts`
3. Set `implemented: true` in `index.ts`
4. Test by enabling the module in `school.config.json`
