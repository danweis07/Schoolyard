# Transportation Module (stub)

This is a stub. To implement:

1. Build pages under `apps/web/src/pages/transportation/`:
   - `index.astro` — bus routes overview
   - `schedule.astro` — pickup/dropoff times
   - `carpool.astro` — carpool board
2. Add a `routes` content collection if you want per-route detail pages
3. Set `implemented: true` in `index.ts`
4. Add transportation-specific i18n keys to `packages/i18n/locales/en.json`
5. Test by enabling the module in `school.config.json`
