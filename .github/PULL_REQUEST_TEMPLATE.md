## Summary

<!-- 1-3 bullet points describing what this PR does and why -->

## Checklist

- [ ] `pnpm typecheck` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm format:check` passes
- [ ] `pnpm validate-config` passes
- [ ] `pnpm build` succeeds
- [ ] `pnpm test` passes
- [ ] `pnpm test:rls` passes (if touching `supabase/migrations/`)
- [ ] New i18n keys added to `en.json` first
- [ ] No hardcoded English strings or colors
- [ ] Accessible: semantic HTML, ARIA labels, keyboard nav
- [ ] Mobile-first responsive (375px minimum)

## Test plan

<!-- How did you verify this works? Steps, screenshots, etc. -->

## Related issues

<!-- Closes #N, Refs #N -->
